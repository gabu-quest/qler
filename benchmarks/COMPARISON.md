# qler vs Celery+Redis: Benchmark Comparison

> Auto-generated by `uv run --group bench python -m benchmarks compare`. Do not edit manually — re-run to update.

## Environment

- **Python**: 3.13.7
- **qler**: 0.5.0
- **SQLite**: 3.50.4
- **Platform**: Linux x86_64 (8 cores)
- **Celery**: 5.6.2
- **redis-py**: 6.4.0
- **Scale**: small (warmup=1, iterations=3)
- **Generated**: 2026-02-27 13:33 UTC

## Caveats (Read First)

Before drawing conclusions, understand what this comparison is and isn't:

1. **Architecture**: qler = same-process SQLite, zero network hops. Celery = separate Redis process, TCP loopback. Different tradeoffs, not just different speeds.
2. **Single machine only**: qler cannot distribute across machines. Celery can. Not tested here.
3. **SQLite write ceiling**: SQLite tops out at ~1-5K writes/sec. Redis handles 100K+. At extreme throughput, Celery wins. We test moderate loads (100-5K jobs).
4. **Solo pool**: Celery uses `--pool solo` (single-threaded) for fair c=1 comparison. Real deployments use `prefork` with multiple worker processes.
5. **Localhost Redis**: No real network latency. Production Redis is often remote (+0.1-1ms per roundtrip). This *understates* the real-world gap.
6. **Cold state**: Fresh DB/Redis each iteration. Production has warm caches and accumulated data.

**Framing**: "Should I add Redis to my stack just for background jobs, or is SQLite enough? If you're on one machine processing < 1K jobs/minute, this shows what you gain (simplicity) and what it costs (throughput ceiling)."

## Results

### Enqueue Latency

Measures the write path: serialize a job and persist (SQLite) or publish (Redis).

| Jobs | System | Median (ms) | P95 (ms) | Throughput (ops/s) | Δ |
|-----:|--------|------------:|---------:|-------------------:|---|
| 100 | qler | 73.68 | — | 1,357 | |
| 100 | celery | 62.36 | — | 1,604 | 1.2x slower |
| 500 | qler | 272.51 | — | 1,835 | |
| 500 | celery | 287.23 | — | 1,741 | 1.1x faster |
| 1000 | qler | 512.33 | — | 1,952 | |
| 1000 | celery | 591.26 | — | 1,691 | 1.2x faster |
| 5000 | qler | 2,723.82 | — | 1,836 | |
| 5000 | celery | 3,128.98 | — | 1,598 | 1.1x faster |

### Batch Enqueue

Bulk submission: `enqueue_many()` (single SQLite transaction) vs `group().apply_async()` (pipelined Redis publishes).

| Jobs | System | Median (ms) | P95 (ms) | Throughput (ops/s) | Δ |
|-----:|--------|------------:|---------:|-------------------:|---|
| 100 | qler | 47.82 | — | 2,091 | |
| 100 | celery | 61.10 | — | 1,637 | 1.3x faster |
| 500 | qler | 99.75 | — | 5,012 | |
| 500 | celery | 310.78 | — | 1,609 | 3.1x faster |
| 1000 | qler | 200.52 | — | 4,987 | |
| 1000 | celery | 634.87 | — | 1,575 | 3.2x faster |
| 5000 | qler | 685.95 | — | 7,289 | |
| 5000 | celery | 3,264.35 | — | 1,532 | 4.8x faster |

### Round-Trip

End-to-end: enqueue → worker claims → executes → returns result. The number that matters for latency-sensitive workloads.

| Jobs | System | Median (ms) | P95 (ms) | Throughput (ops/s) | Δ |
|-----:|--------|------------:|---------:|-------------------:|---|
| 100 | qler | 333.69 | — | 299.7 | |
| 100 | celery | 174.88 | — | 571.8 | 1.9x slower |
| 500 | qler | 1,627.64 | — | 307.2 | |
| 500 | celery | 859.51 | — | 581.7 | 1.9x slower |
| 1000 | qler | 3,171.97 | — | 315.3 | |
| 1000 | celery | 1,709.11 | — | 585.1 | 1.9x slower |

### Sustained Throughput

Sequential enqueue-process-complete cycles. The number that matters for batch workloads.

| Jobs | System | Median (ms) | P95 (ms) | Throughput (ops/s) | Δ |
|-----:|--------|------------:|---------:|-------------------:|---|
| 100 | qler | 333.21 | — | 300.1 | |
| 100 | celery | 170.73 | — | 585.7 | 2.0x slower |
| 500 | qler | 1,587.45 | — | 315.0 | |
| 500 | celery | 857.48 | — | 583.1 | 1.9x slower |
| 1000 | qler | 3,132.86 | — | 319.2 | |
| 1000 | celery | 1,698.34 | — | 588.8 | 1.8x slower |
| 5000 | qler | 15,600.34 | — | 320.5 | |
| 5000 | celery | 8,343.25 | — | 599.3 | 1.9x slower |

## Analysis

**Enqueue latency**: qler's `enqueue()` is faster than Celery's `apply_async()` at most scale points (~1.1x average). A local SQLite write avoids TCP round-trips to Redis.

**Batch enqueue**: qler's `enqueue_many()` dominates (~3.1x average). A single SQLite transaction for the entire batch beats individual Redis publishes. The advantage grows with batch size.

**Round-trip**: Celery wins the end-to-end race (~1.9x average). qler's 3 SQLite writes per job (enqueue + claim + complete) add up. Celery's Redis-backed broker + in-memory result backend is faster per operation, even with TCP overhead.

**Throughput**: Celery sustains higher throughput (~1.9x average). Redis's in-memory operations handle sustained load better than SQLite's disk-backed writes. Note: Celery uses `--pool solo` (c=1) here — real deployments with `prefork` would widen this gap further.

## When to Use Which

| Scenario | Recommendation |
|----------|---------------|
| Single machine, < 1K jobs/min | **qler** — no infrastructure, same-process, debuggable |
| Single machine, > 5K jobs/min | **Celery+Redis** — SQLite write ceiling becomes a bottleneck |
| Multi-machine / distributed | **Celery+Redis** — qler can't distribute |
| Development / prototyping | **qler** — zero setup, `pip install qler` and go |
| Already running Redis | **Celery** — marginal cost of adding a queue is near zero |
| Debugging / observability first | **qler** — full SQL access to every job, attempt, failure |

---

*Generated by the qler benchmark suite. Run `uv run --group bench python -m benchmarks compare` to regenerate.*
