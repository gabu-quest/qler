# Commands (Legacy)

Commands are legacy. **Skills are the preferred mechanism** and should be used for all new behavior.

When a command and a skill share the same name, **the skill takes precedence**.

## Where to add new behavior
Add or update skills in `.claude/skills/<skill-name>/SKILL.md`.
